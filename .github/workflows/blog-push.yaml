name: blogPush

on:
  push:
    branches: [ main ]

  workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run a one-line code 
      run: echo Hellow word!!!!